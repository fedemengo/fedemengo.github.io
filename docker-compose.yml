services:
  jekyll:
    image: ghcr.io/fedemengo/al-folio:1.0.0
    working_dir: /srv/jekyll
    volumes:
      - .:/srv/jekyll
    ports:
      - 8080:8080
      - 35729:35729
    command: jekyll serve
      --host=0.0.0.0
      --port=8080
      --livereload
      --force_polling

